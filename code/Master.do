* 1.Process of dealing with data
** (1) Read and fix the first raw data (containing population and GDP per capita and life expectancy)
do code/Read and fix data.do

** (2) Deal with the second new raw data (containing government health expenditure per capita)
do code/Deal with raw data(expen).do

** (3) Aggregation(the above two dta.files), filter and transforamtion
do code/Aggregation, filter and transforamtion.do

* 2.Run OLS and create a graph
do code/OLS and graph.do

* 3. Automate repeating tasks using Stata "for" loops
do code/Aggregation repeating tasks.do

* 4. Install packages
do code/Install packages.do
